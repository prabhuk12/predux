class EventManager
{
    public subscribeSheetEvents(inId:string, event:Event, sheetId:string)
    {
        // find the insight
        // find the sheet the insight
        // subscribe to that event
    }

}

export default EventManager;